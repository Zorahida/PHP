{% extends "Music/baseMusic.html.twig" %}

{% block contenido %}
    <h2>Listado de Metallica </h2>
    <div class="row row-cols-1 row-cols-md-2">
        {% for music in music %}
            <div class="col mb-4">
                <div class="card" style="width: 18rem;">
                    <img src="{{ music.pic }}" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">{{ music.name }}</h5>
                        <p class="card-text">{{ music.instrument }}</p>
                        <a href="{{ path('getMusics', {'age': music.age}) }}" class="btn btn-primary">Más Info</a>
                        <a href="{{ path('/member', {'name': music.name}) }}" class="btn 
                        btn-primary">Editar</a>
                        
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
