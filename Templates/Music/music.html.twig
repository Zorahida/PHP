{% extends "Music/baseMusic.html.twig" %}

{% block contenido %}
    <h2>Metallica!!</h2>
    <div class="row row-cols-1 row-cols-md-2">
        {% for pepino in music %}
            <div class="col mb-4">
                <div class="card" style="width: 18rem;">
                    <img src="{{pepino.pic}}">
                    <div class="card-body">
                        <h5 class="card-title">{{ pepino.name }}</h5>
                        <p class="card-text">{{ pepino.instrument }}</p>
                        <p class="card-text">{{ pepino.origin }}</p>
                        <p class="card-text">{{ pepino.age }}</p>
                        <a href="#" class="btn btn-primary">Más Info</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
